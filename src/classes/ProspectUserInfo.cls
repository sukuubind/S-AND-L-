public with sharing class ProspectUserInfo {
		
	/*	
  public user currentuser{get;set;}
  
  public ProspectUserInfo(ApexPages.StandardController cont){
   
     currentuser = new User();
     currentuser=[Select Id,Name, u.Profile.Name from User where Id=:userinfo.getuserId()];
  } 
  
  */
}